<!DOCTYPE html>
<html>
  <head>
    <title>apiFun</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>

    <nav class="pt-4 pl-4 pr-4 flex">

      <h1 class="text-4xl font-bold text-blue-600">apiFun</h1>

      <ul class="flex border-b">
        <li class="-mb-px mr-1 ml-12">
          <%= link_to 'Home', root_path, class: "py-2 px-4 inline-block border-l border-t border-r rounded-t #{current_page?(root_path) ? 'bg-blue-600 text-white' : 'bg-white'}" %>
        </li>
        <li class="-mb-px mr-1">
          <%= link_to 'News Search', news_view_path, class: "py-2 px-4 inline-block border-l border-t border-r rounded-t #{current_page?(news_view_path) ? 'bg-blue-600 text-white' : 'bg-white'}" %>
        </li>
        <li class="-mb-px mr-1">
          <%= link_to 'Nookal Clients', nookal_view_path, class: "py-2 px-4 inline-block border-l border-t border-r rounded-t #{current_page?(nookal_view_path) ? 'bg-blue-600 text-white' : 'bg-white'}" %>
        </li>
        <% if user_signed_in? %> <!-- Check if user is signed in -->
          <li class="-mb-px">
            <%= button_to 'Logout', destroy_user_session_path, method: :delete, class: "py-2 px-4 hover:bg-blue-100 inline-block border-l border-t border-r rounded-t bg-white" %>
          </li>
        <% end %>
      </ul>
    </nav>
    <div class="bg-blue-600 h-1 w-full"></div>
    
    <main class="container mx-auto mt-28 px-5 flex">
      <%= yield %>
    </main>
  </body>
</html>
